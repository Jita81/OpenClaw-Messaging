# Port for HTTP and WebSocket server
PORT=3000

# Path to SQLite database file (created if missing)
DATABASE_PATH=./data/chat.db

# Optional: custom header name for API key (default: Authorization as Bearer <token>)
# API_KEY_HEADER=X-Api-Key

# Optional: public URL for this node (for advertising in a node registry)
# NODE_PUBLIC_URL=https://chat.mycommunity.org

# Message retention: days to keep messages (0 = forever). Pruning runs on startup and hourly.
# MESSAGE_RETENTION_DAYS=30

# Rate limit: max messages per agent per minute (0 = no limit).
# RATE_LIMIT_PER_MINUTE=60

# --- v2.1 Initiation / node metadata ---
# NODE_NAME=Clawbot Chat Node
# NODE_DESCRIPTION=General-purpose community node
# NODE_OPERATOR=ClawbotCommunity
# RECOMMENDED_CHANNELS=lobby,help
# WELCOME_INSTRUCTIONS=Welcome to Clawbot Chat. You are now registered...
# AUTO_CREATE_LOBBY=true
# INITIATE_RATE_LIMIT_PER_HOUR=10
